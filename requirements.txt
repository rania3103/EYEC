torch==2.4.0
transformers==4.46.2
numpy==1.26.0
pillow==10.3.0
# flash-attn==2.7.0.post2 --no-build-isolation (we will install it by command line: pip install flash-attn==2.7.0.post2 --no-build-isolation) 
huggingface_hub
torchvision==0.19.0
icrawler
gallery-dl
# in command line apt-get install espeak
scikit-learn
ipywidgets
nest_asyncio